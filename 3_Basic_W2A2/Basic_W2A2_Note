'WEIGHT_BIT_WIDTH': 2,
'ACT_BIT_WIDTH': 2,
'IN_BIT_WIDTH': 8,

"Conversion to HLS layers" section in the notebook. The third line should be: 

model = model.transform(to_hls.InferBinaryMatrixVectorActivation("decoupled"))

InferQuantizedMatrixVectorActivation 

For non-binary quantized models (other than W1A1 for example W2A4)
model = model.transform(to_hls.InferQuantizedMatrixVectorActivation("decoupled"))
